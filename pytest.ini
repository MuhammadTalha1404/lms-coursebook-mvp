[pytest]
testpaths = tests
pythonpath = .
addopts = -ra
markers =
    integration: requires a dedicated, running MySQL test database
    ui: requires Streamlit and a dedicated, running MySQL test database
